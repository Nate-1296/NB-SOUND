# Dependencias para builds de distribución oficial (bundles PyInstaller).
#
# Excluye torch, torchaudio y demucs para mantener los artefactos bajo el
# límite de 2 GB por archivo de GitHub Releases (~330 MB/bundle en lugar de ~2.7 GB).
# Audio features (librosa), análisis acústico, búsqueda e IA de desempate
# quedan totalmente operativos. El karaoke muestra un aviso en la UI.
#
# Para desarrollo o builds con karaoke completo: pip install -r requirements.txt

musicbrainzngs==0.7.1
mutagen==1.47.0
python-dotenv>=1.0.0
PySide6>=6.6.0
Pillow>=10.0.0
pyacoustid>=1.3.0
shazamio>=0.4.0
anthropic>=0.25.0
openai>=1.0.0
python-vlc>=3.0.20123
numpy>=1.26.0
soundfile>=0.12.0
librosa>=0.11.0
